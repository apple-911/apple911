
文档会先给出**页面总览**，然后按照之前的功能模块，逐一细化每个页面的**布局、核心组件、交互行为和关键字段**。所有内容都采用结构化描述，AI 可以直接依据此生成可运行的前端代码或高保真原型。

---

# MDT 系统原型方案

## 0. 全局约定

- **布局风格**：左右结构（左侧菜单导航，右侧内容区域）。顶部为系统名称、用户头像/通知。
- **配色**：医疗蓝+白色为主，强调部分用绿色/橙色。
- **响应式**：PC端优先，兼顾平板。
- **通用组件**：表格（支持搜索/筛选/分页）、表单（带验证）、弹窗（模态框）、消息提示。

---

## 1. 页面清单（共 23 个核心页面）

| 模块 | 页面名称 | 路由路径 | 权限角色 |
|------|----------|----------|-----------|
| 公共 | 登录页 | `/login` | 所有 |
| 公共 | 工作台首页 | `/dashboard` | 所有 |
| 会诊核心 | 申请会诊 | `/consultation/apply` | 申请医生 |
| 会诊核心 | 我的申请列表 | `/consultation/my-applies` | 申请医生 |
| 会诊核心 | 待审核列表 | `/consultation/pending-review` | MDT秘书/管理员 |
| 会诊核心 | 会诊排期管理 | `/consultation/schedule` | MDT秘书 |
| 会诊核心 | 我的待参会 | `/consultation/my-meetings` | 会诊专家 |
| 会诊核心 | 会诊详情页 | `/consultation/detail/:id` | 所有参与角色 |
| 患者管理 | 患者档案库 | `/patient/list` | 申请医生/秘书/专家 |
| 患者管理 | 患者360视图 | `/patient/360/:id` | 同上 |
| 会诊协同 | 虚拟会诊室 | `/consultation/room/:id` | 参会人员 |
| 会诊协同 | 床边会诊辅助 | `/consultation/bedside/:id` | 床边终端 |
| 诊后与质控 | 会诊报告管理 | `/report/list` | 专家/秘书 |
| 诊后与质控 | 报告编辑与签名 | `/report/edit/:id` | 专家 |
| 诊后与质控 | 随访计划管理 | `/followup/list` | 专家/秘书 |
| 诊后与质控 | 疗效评估录入 | `/followup/assessment` | 责任医生 |
| 诊后与质控 | 统计分析看板 | `/statistics` | 质控员/管理者 |
| 诊后与质控 | 质控任务审核 | `/quality/tasks` | 质控员 |
| 移动协作 | 移动端会诊首页 | `/m/home` | 所有（手机版） |
| 移动协作 | 移动端会诊室 | `/m/room/:id` | 参会人员 |
| 系统基础 | 专家库管理 | `/admin/expert-list` | 系统管理员 |
| 系统基础 | 团队管理 | `/admin/team-list` | 系统管理员 |
| 系统基础 | 角色权限配置 | `/admin/roles` | 系统管理员 |
| 系统基础 | 系统日志 | `/admin/logs` | 系统管理员 |

> *注：移动协作模块页面与 PC 端功能对应，但采用移动适配布局，单独列出以提示生成响应式版本。*

---

## 2. 页面细化描述（按模块）

### 2.1 会诊核心模块

#### 页面1：申请会诊 `/consultation/apply`

- **布局**：左右两栏（左侧表单，右侧患者快速检索）。
- **核心组件**：
  - 患者检索/选择：输入姓名/住院号 → 自动匹配患者列表（下拉选择）。
  - 多步表单（Step1：基本信息；Step2：病情摘要；Step3：邀请专家；Step4：期望时间）。
  - 邀请专家组件：按科室/职称筛选，支持多选，展示专家头像+姓名+科室+忙闲状态（绿/红）。
  - 病情附件上传：支持图片/PDF/DICOM，多文件批量上传。
  - 提交按钮：点击后生成“待审核”申请单。
- **交互**：表单实时校验；提交成功后弹窗提示，并跳转至“我的申请列表”。

#### 页面2：我的申请列表 `/consultation/my-applies`

- **布局**：标准表格 + 顶部筛选栏（状态、日期范围）。
- **表格列**：申请单号、患者姓名、会诊类型（院内/远程）、申请时间、期望时间、当前状态（待审核/已通过/已拒绝/已完成）、操作（查看详情、撤销）。
- **操作**：点击“查看详情”进入会诊详情页；状态为“待审核”时可撤销（弹窗二次确认）。

#### 页面3：待审核列表 `/consultation/pending-review`（MDT秘书专用）

- **布局**：卡片列表模式（每项申请显示摘要），支持批量操作。
- **每张卡片**：患者信息、申请医生、科室、期望时间、邀请专家名单（预览前3人）、紧急程度（红/黄标）。
- **操作按钮**：通过、拒绝、修改排期（打开排期弹窗）。
- **交互**：通过后自动进入“会诊排期管理”页面，并可一键向专家发送通知。

#### 页面4：会诊排期管理 `/consultation/schedule`

- **布局**：周视图日历（类似Outlook） + 右侧待排期列表。
- **日历格**：显示已有会诊（标题+专家数量），支持拖拽调整时间。
- **待排期列表**：展示未排期的已通过申请，点击“快速排期”弹出时间选择器，自动标出所有专家共同空闲时段（通过颜色块提示）。
- **交互**：选定时段后确认 → 生成正式会诊事件 → 自动推送通知给所有专家。

#### 页面5：我的待参会 `/consultation/my-meetings`（专家专用）

- **布局**：列表 + 今日/本周/未来切换标签。
- **列表项**：会诊主题、患者（匿名/脱敏展示部分信息，如“李**”）、时间、地点/线上链接、状态（未开始/进行中/已结束）。
- **操作**：进入会诊室（按钮状态根据时间激活）；可填写预审意见（弹窗）。

#### 页面6：会诊详情页 `/consultation/detail/:id`

- **布局**：上下结构（上方患者摘要，下方四个标签页：会诊信息、资料库、讨论记录、报告）。
- **患者摘要栏**：姓名、年龄、门诊号/住院号、主要诊断（可点击查看360视图）。
- **标签页**：
  - 会诊信息：申请单信息、专家团队列表（每位专家签到状态）、会诊时间、地点/会议链接。
  - 资料库：展示所有上传的病历、影像、检验报告，支持在线预览。
  - 讨论记录：展示语音转文字摘要（或手动记录），按时间线排列。
  - 报告：会诊报告草稿/终稿，以及下载/分享按钮。
- **操作**：对于进行中的会诊，显示“进入会诊室”悬浮按钮。

---

### 2.2 患者管理模块

#### 页面7：患者档案库 `/patient/list`

- **布局**：搜索栏（姓名/住院号/身份证）+ 高级筛选（科室、主治医生） + 表格。
- **表格列**：姓名（可点击）、性别、年龄、住院号、主要诊断、最近会诊时间、操作（查看360、发起会诊）。
- **交互**：点击姓名或“查看360”进入患者360视图。

#### 页面8：患者360视图 `/patient/360/:id`

- **布局**：左侧时间轴（里程碑事件：入院、会诊、手术、出院等），右侧内容面板。
- **右侧内容**：
  - 基本信息卡片：联系方式、过敏史、既往史等。
  - 诊疗时间轴视图（甘特图样式）展示检查、用药、会诊记录。
  - 检查报告聚合：按类型（影像/病理/检验）折叠卡片，支持点击查看原图。
  - 会诊历史卡片：每次会诊的专家结论摘要及报告链接。
- **交互**：点击报告链接跳转到报告详情，点击“发起新会诊”会预填患者信息。

---

### 2.3 会诊协同模块

#### 页面9：虚拟会诊室 `/consultation/room/:id`

- **布局**：左中右三栏（左侧参会者列表+聊天，中间视频/白板区域，右侧资料共享区）。
- **中间区域**：
  - 默认开启主讲人视频（或共享屏幕），支持切换布局（画廊模式/演讲者模式）。
  - 工具栏：麦克风、摄像头、屏幕共享、白板、录制。
- **右侧资料共享区**：
  - 当前展示的病历/影像（可从“资料库”拖拽到中间白板区域进行标注）。
  - 支持多方同时标注（显示不同颜色光标）。
- **左侧**：
  - 参会者列表（显示姓名、角色、音视频状态）。
  - 文字聊天框（可@某人）。
- **额外功能**：弹出现场录音转写面板（实时生成讨论文本），专家可插入标记作为会诊纪要。

#### 页面10：床边会诊辅助 `/consultation/bedside/:id`（用于移动推车/PAD）

- **布局**：全屏视频通话 + 悬浮工具栏（最小化）。
- **特性**：
  - 自动连接床边摄像头，支持放大/缩小画面。
  - 专家端可远程控制床边摄像头云台（左/右/上下）。
  - 床边医生可拍照上传局部体征照片，即时同步到虚拟会诊室。
- **交互**：一键调取患者实时生命体征（对接HIS）。

---

### 2.4 诊后与质控模块

#### 页面11：会诊报告管理 `/report/list`

- **布局**：双列表（左侧未完成报告列表，右侧已完成报告列表）。
- **未完成报告**：显示会诊ID、患者、会诊时间、负责专家、操作（编写报告）。
- **已完成报告**：显示报告状态（草稿/已签名）、操作（查看/打印）。
- **交互**：点击“编写报告”进入报告编辑页。

#### 页面12：报告编辑与签名 `/report/edit/:id`

- **布局**：富文本编辑器（支持表格、图片插入）+ 右侧结构化字段区（会诊意见、诊疗建议、随访建议）。
- **签名区**：显示所有与会专家姓名，每个专家旁有“电子签名”按钮（需二次密码/扫码验证）。
- **操作**：暂存草稿、提交审核、完成签名（全体签名后方可归档）。

#### 页面13：随访计划管理 `/followup/list`

- **布局**：以患者为中心的计划列表，上方创建新计划按钮。
- **创建/编辑计划弹窗**：选择患者、随访目的、随访节点（时间点+任务形式：电话/复诊/问卷）、执行医生。
- **表格列**：患者、计划周期、下次随访时间、最近执行状态、操作（编辑/终止）。
- **交互**：到期自动向责任医生发送待办通知。

#### 页面14：疗效评估录入 `/followup/assessment`

- **布局**：两步向导（选择随访患者 → 填写评估表单）。
- **评估表单**：基于预设量表（如肿瘤患者的RECIST、卡氏评分），支持分值计算和结论自动生成。
- **操作**：保存后更新到患者360视图的时间轴。

#### 页面15：统计分析看板 `/statistics`

- **布局**：顶部指标卡片（会诊总数、平均响应时间、专家参与率、申请科室排名），下方图表区域。
- **图表组件**：
  - 折线图：每月会诊量趋势。
  - 饼图：会诊类型分布（院内/远程）。
  - 条形图：各科室平均报告完成时间。
- **交互**：点击图表下钻显示详细清单（弹窗表格）。

#### 页面16：质控任务审核 `/quality/tasks`

- **布局**：任务列表（按抽查时间段 + 会诊ID），每个任务需填写质控评分（1-5分）。
- **质控表单**：包含文书完整性、指南依从性、专家参与度等指标。
- **操作**：提交后生成质控报告，并可退回要求整改。

---

### 2.5 移动协作模块（采用移动端垂直布局，底部Tab栏）

#### 页面17：移动端会诊首页 `/m/home`

- **底部Tab**：首页、消息、待办、我的。
- **首页**：显示今日会诊卡片（倒计时）、待处理申请数量、最近访问的患者。
- **消息**：系统通知（会诊邀请、报告待签）。
- **待办**：统一显示所有需要当前用户处理的任务（审核、排期、参会、签报告）。

#### 页面18：移动端会诊室 `/m/room/:id`

- **布局**：竖屏优化，默认全屏视频（参与者头像悬浮），底部操作栏。
- **底部栏**：麦克风、摄像头、共享屏幕、参与者列表、聊天。
- **聊天窗**：滑动抽屉从右侧弹出。
- **特殊功能**：点击“资料”可快速翻阅患者影像，并支持指画标注（发送到桌面端共享区）。

---

### 2.6 系统基础模块

#### 页面19：专家库管理 `/admin/expert-list`

- **布局**：可编辑表格 + 新增专家弹窗。
- **表格列**：姓名、科室、职称、专业擅长、忙闲状态（手动或自动同步排班）、操作（编辑/禁用）。
- **新增/编辑弹窗**：包含基本个人信息、执业证书附件、签到方式（人脸/密码）。
- **批量操作**：导入/导出专家信息（Excel）。

#### 页面20：团队管理 `/admin/team-list`

- **布局**：树形结构（团队-成员），左侧团队列表，右侧成员列表。
- **操作**：创建MDT团队（如“肺癌MDT团队”），将专家库中的成员添加至团队，指定团队负责人。
- **交互**：每个团队可设置默认的会诊规则（如至少3位不同科室专家）。

#### 页面21：角色权限配置 `/admin/roles`

- **布局**：左侧角色列表（申请医生、MDT秘书、专家、科室主任、质控员、系统管理员），右侧权限树（按页面/按钮勾选）。
- **操作**：可自定义新增角色，复制已有角色权限。
- **实时生效**：保存后当前在线用户权限更新（提示重新登录）。

#### 页面22：系统日志 `/admin/logs`

- **布局**：日志表格 + 搜索（时间、操作人、操作类型）。
- **表格列**：时间、操作人IP、操作行为（如“用户[张三]申请会诊#10086”）、结果。
- **交互**：支持按会诊ID关联查看上下文日志。

---

### 2.7 公共页面

#### 页面23：登录页 `/login`

- **布局**：居中卡片形式。
- **组件**：用户名/手机号、密码、验证码（滑动验证或图形码）、登录按钮。
- **选项**：支持扫码登录（企业微信/钉钉集成提示）。
- **交互**：错误提示、记住密码复选框、忘记密码链接（走短信重置）。

#### 页面24：工作台首页 `/dashboard`

- **布局**：根据不同角色展示不同卡片集合。
- **通用卡片**：
  - 待办事项（待审核/待参会/待签报告数量）。
  - 今日会诊（列表形式，点击快速进入会诊室）。
  - 公告与通知（滚动列表）。
- **管理员专属卡片**：系统运行状况（在线专家数、今日活跃会诊）。

---

## 3. 补充说明（给Trae的提示）

- **数据交互**：所有列表数据使用模拟 JSON 数据即可，但需要预留 API 接口格式（例如：`/api/consultations`）。请为每个页面生成 mock 数据样例。
- **状态同步**：同一个会诊在不同页面（申请、排期、详情）的状态需保持一致，请在代码中模拟全局状态管理（如 Vuex / Redux）。
- **移动端适配**：移动端页面应使用 viewport 设置，采用 Flex/Grid 布局，并适配 touch 事件。
- **权限模拟**：登录时可选择角色（前端模拟不同角色视图），以便演示不同权限看到的菜单和按钮。
- **代码技术栈建议**：使用 Vue 3 + Element Plus / React + Ant Design，生成可运行的组件代码。如果 Trae 不支持指定框架，请按常见 UI 库输出 HTML/CSS/JS。

---

以上方案已经详细分层、分页面、分组件，并明确了交互逻辑。你可以直接将这份文档提供给 Trae，并要求它：“根据上述原型方案，生成一套完整的 MDT 系统前端原型页面，包含所有路由和模拟数据，实现可交互演示”。


**技术实现要求**：

- 前端框架：React 18 + TypeScript
    
- UI 组件库：Ant Design 5.x（使用 `@ant-design/icons`、`@ant-design/pro-components` 可选）
    
- 状态管理：Zustand（简单跨页面状态，如当前用户角色、会诊列表过滤条件）
    
- 路由：React Router v6，实现嵌套布局和权限模拟（通过 `role` 查询参数切换角色视图）
    
- 样式：Tailwind CSS 辅助布局，结合 Ant Design 默认主题（修改 token 主色为 `#1677ff` 或医疗蓝 `#2c6e9e`）
    
- 模拟数据：使用 `mockjs` 生成患者列表、会诊申请、专家库等数据，并提供 `setTimeout` 模拟异步请求
    
- 移动端页面：单独路由前缀 `/m`，采用 `antd-mobile`（或继续使用 antd 但用响应式 grid + 移动适配）
    
- 代码输出：每个页面生成独立的 `.tsx` 文件，使用函数组件 + hooks，包含完整 JSX 和样式。提供 `App.tsx` 和路由配置入口。

## 目录结构示意（送给 Trae 的骨架）

src/
├── api/                  # 模拟 API 或真实请求
│   └── consultation.ts
├── assets/               # 图片、全局样式
├── components/           # 通用组件（例如 ExpertSelector, PatientCard）
├── layouts/              # 布局（MainLayout, BlankLayout）
├── pages/                # 所有页面（按模块目录）
│   ├── consultation/
│   │   ├── Apply/
│   │   ├── MyApplies/
│   │   ├── PendingReview/
│   │   ├── Schedule/
│   │   ├── MyMeetings/
│   │   └── Detail/
│   ├── patient/
│   │   ├── List/
│   │   └── Patient360/
│   ├── report/...
│   ├── followup/...
│   ├── quality/...
│   ├── admin/...
│   └── m/ (移动端页面)
├── routes/               # 路由配置 + 权限映射
├── stores/               # Zustand/Redux stores
├── utils/                # 请求拦截、日期格式化等
├── mocks/                # mock 数据定义
├── App.tsx
└── main.tsx